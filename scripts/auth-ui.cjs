const fs=require('fs');let p='apps/web/components/Marketplace.tsx';let s=fs.readFileSync(p,'utf8');s=s.replace("function Login(){const {t,locale}=useText();", "function Login(){const {t,locale}=useText();const [auth,setAuth]=useState<Data|null>(null);useEffect(()=>{api('/auth/config').then(setAuth);},[]);");s=s.replace("<h1>{t('login')}</h1><p>{t('loginText')}</p><form", "<h1>{t('login')}</h1><p>{t('loginText')}</p>{auth?.oidc&&<a className=\"button\" href=\"/api/auth/oidc\">{t('signIn')} · SSO / MFA</a>}<form");s=s.replace("<input name=\"password\" type=\"password\"", "<input name=\"password\" type=\"password\"");fs.writeFileSync(p,s);
